PATCH INSTRUCTIONS

1. 本次修复说明
   - 修复：管理员在查看用户页面时的“返回后台”按钮体验问题，包括不在用户“我的”页显示、改为圆形小图标并向上偏移、回跳优先使用 sessionStorage.lastAdminUrl、修正退出工作台的跳转目标。

2. 本地复现步骤（手动）
   - 开发方便：当前 `navbar.js` 会在本地自动设置 `userRole='admin'`（仅用于开发，便于快速验证），你也可以手动执行：
     - `localStorage.setItem('userRole','admin')`  // 模拟管理员身份
   - 打开任何用户页面（非“我的”，例如：`主界面/用户/用户“首页”界面.html`），确认右下方出现红色圆形返回按钮（管理员身份可见）。
   - 点击该按钮：
     - 若之前从管理员页面点击“退出工作台”而来到该用户页面，则应该回跳到管理员来源页面（优先使用 `sessionStorage.lastAdminUrl`）。
     - 若无来源，则 **回退到管理员首页**（`../管理员/管理员首页.html`）。

3. 登录页行为（新增说明）
   - 管理员登录页（`注册及登录/管理员登录界面.html`）在成功登录后会写入 `localStorage.userRole='admin'` 并跳转到 `../主界面/管理员/管理员首页.html`。
   - 普通用户登录页（`注册及登录/用户登录界面.html`）在成功登录后会写入 `localStorage.userRole='user'` 并跳转到 `../主界面/用户/用户“首页”界面.html`。


3. 自动 smoke-test（简单）
   - 可使用任意静态服务器（例如 `npx http-server` 或自带的 `simple-serve.js` 若存在）启动并访问以下页面，确保 HTTP 200：
     - 主界面/用户/用户“首页”界面.html
     - 主界面/用户/用户“食堂”界面.html
     - 主界面/用户/用户“社区”界面.html
     - 主界面/用户/用户“ai助手”界面.html
     - 主界面/用户/用户“我的”界面.html

4. 回滚
   - 若需要回退本次更改，请将本次提交（或补丁）还原，或撤销对以下文件的修改：
     - `主界面/用户/用户“首页”界面.html`
     - `主界面/用户/用户“社区”界面.html`
     - `主界面/用户/用户“食堂”界面.html`
     - `主界面/用户/用户“ai助手”界面.html`
     - `主界面/用户/用户“我的”界面.html`
     - `主界面/管理员/navbar.js`

5. 备注
   - 所有更改均为前端安全性 UX 改进；后端不需要任何更改。